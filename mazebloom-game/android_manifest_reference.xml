<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- INTERNET permission — Ad দেখাতে এবং network access এর জন্য দরকার -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:label="maze_bloom"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <!-- AdMob App ID — এটা না দিলে app crash করবে -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713"/>

        <!-- নতুন "Impeller" renderer অনেক mid/low-end Android GPU (বিশেষ করে
             Mali) তে gradient + shadow + text একসাথে থাকা widget গুলোতে text/icon
             অদৃশ্য হয়ে যাওয়া এবং মাঝে মাঝে স্ক্রিন black হয়ে freeze হয়ে যাওয়ার
             মতো রেন্ডারিং bug তৈরি করে (এটা Flutter এর নিজের known issue) —
             পুরোনো, বেশি স্থিতিশীল Skia renderer ব্যবহার করতে এটা off করা হলো -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="false"/>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
